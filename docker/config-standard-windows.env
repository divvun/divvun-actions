# Configuration for standard Windows build servers
# Source this file before running update.ps1 or use with update-cron.ps1

# Container configuration
$env:INSTANCE_COUNT = 2
$env:CONTAINER_PREFIX = "builder-"
$env:QUEUE_TAGS = "queue=windows"

# Required: Set your Buildkite agent token
# $env:BUILDKITE_AGENT_TOKEN = "your-token-here"