#!/bin/sh
SYSROOT="/opt/sysroot-aarch64"
MUSL_TARGET="aarch64-linux-musl"

exec clang++ \
    --target=${MUSL_TARGET} \
    --sysroot=${SYSROOT} \
    -stdlib=libc++ \
    --rtlib=compiler-rt \
    --unwindlib=libunwind \
    -Wno-unused-command-line-argument \
    "$@"
